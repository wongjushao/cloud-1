---
all:
  children:
    inception:
      hosts:
        server1:
          ansible_host: 47.237.113.200
        server2:
          ansible_host: 47.237.252.35
      vars:
        ansible_user: "{{ lookup('env', 'ANSIBLE_USER') }}"
        ansible_ssh_private_key_file: "{{ lookup('env', 'ANSIBLE_SSH_PRIVATE_KEY_FILE') }}"
